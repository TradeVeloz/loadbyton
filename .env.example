PORT=4100
# JWT_SECRET is auto-generated on first run if you leave this unset — but set a
# fixed value in production, otherwise every restart invalidates all sessions.
JWT_SECRET=
# Where the SQLite database and uploaded documents live. Defaults to ./data.
# Point this at a mounted persistent volume in production (e.g. Railway volumes).
DATA_DIR=
